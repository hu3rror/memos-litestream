name: Upstream stable image

on:
  push:
    paths:
      - 'Dockerfile'
  workflow_dispatch:

jobs:
  build:
    uses: ./.github/workflows/build-and-push.yml
    with:
      tag: stable
      add_version_tag: true